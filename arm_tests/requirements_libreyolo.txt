# LibreYOLO Inference Benchmark Requirements
#
# Install core:
#   pip install -r requirements_libreyolo.txt
#
# Install all optional extras in one shot:
#   pip install libreyolo[rfdetr]    # RF-DETR detection + segmentation
#   pip install libreyolo[tensorrt]  # TensorRT .engine inference
#   pip install libreyolo[onnx]      # ONNX Runtime inference / export
#   pip install libreyolo[openvino]  # OpenVINO inference / export


# ── Core ──────────────────────────────────────────────────────────────────────
libreyolo>=0.1.4
torch>=1.7.0
torchvision>=0.8.0
opencv-python>=4.5.0
numpy>=1.19.0,<2.0.0
pyyaml>=6.0

# Performance monitoring
psutil>=5.8.0


# ── RF-DETR detection + segmentation ─────────────────────────────────────────
# pip install libreyolo[rfdetr]
# Installs the packages listed below:
#
# rfdetr>=1.6.4               # RT-DETR backbone + head
# supervision>=0.27.0         # Detection / mask utilities used by rfdetr
# accelerate>=1.13.0          # HuggingFace accelerate (rfdetr training)
# peft>=0.19.1                # Parameter-efficient fine-tuning (rfdetr)
# pyDeprecate>=0.5.0          # rfdetr dependency
# transformers                # HuggingFace Transformers (rfdetr backbone)
# timm                        # PyTorch image models  (rfdetr encoder)


# ── TensorRT (.engine) ────────────────────────────────────────────────────────
# pip install libreyolo[tensorrt]
# NOTE: TensorRT itself must be installed separately matching your CUDA version.
#       On Jetson, TensorRT is pre-installed with JetPack.
#       On x86 NVIDIA systems: https://developer.nvidia.com/tensorrt
#
# pycuda                      # Python bindings for CUDA / TensorRT


# ── ONNX Runtime ──────────────────────────────────────────────────────────────
# pip install libreyolo[onnx]
#
# onnxruntime>=1.10.0         # CPU ONNX inference
# onnxruntime-gpu             # GPU ONNX inference (alternative to above)
# onnx>=1.11.0                # ONNX model manipulation
# onnxsim>=0.4.0              # ONNX graph simplification
# onnxscript                  # ONNX scripting utilities


# ── OpenVINO ──────────────────────────────────────────────────────────────────
# pip install libreyolo[openvino]
#
# openvino>=2023.0.0          # OpenVINO runtime (Intel CPU / GPU / VPU)
# openvino-dev>=2023.0.0      # Model Optimizer and benchmark tools
